# 草稿後畫面排版與送出進度

- 使用者要求改善建立問題草稿後的畫面，並回報兩位學生送出後完成率仍顯示 0。
- 唯讀查看 AI量化交易課堂：原畫面上方課程標題占用大量高度，題目區再分割回答與進度，下一步操作位於內容下方。學生已成功加入新課堂。
- 正式資料確認兩組回答在 2026-09-07T05:57:23.307Z、05:57:48.705Z 提交，後續在 05:58:37.234Z 鎖定。讀取時已有兩份，未更動或補造提交；無法僅憑紀錄確定使用者分頁停在 0 的完整原因。近期有多次取消的讀取請求及一次 409，不能據此當作成功提交。
- 教師背景更新從每 6 秒改為每 2 秒，持續取得進度，不再隨 pending 變化重設間隔；返回可見頁面或視窗取得焦點時立即更新。沿用單一讀取進行中的跳過機制，避免重複請求互相中斷。學生仍保留原 6／8 秒間隔。
- 教師成功讀取時套用 serverNow，以便進度區顯示資料更新時間。既有「即時完成率」以實際內容改為「已送出組數」，顯示每 2 秒更新提示。
- 縮減課程標題與草稿留白；下一步操作靠近題目標題，回答與進度改為上下排列，回答卡片依可用寬度排列；窄螢幕先顯示目前題目，再顯示問題清單。
- npm run test:unit：83 項通過，新增 0→1→2 送出組數更新，以及教師每 2 秒、切回頁面／視窗、清理計時器的驗證。npm run gate:static 通過。建置及正式畫面檢視另於部署流程確認。
- 只更改顯示與讀取排程；未操作正式課堂的作答、排序或階段。來源另建立提交，推送原 Sites 來源庫並部署。

## 公布後分開顯示兩份排序

- 使用者要求最高到最低，分別顯示全班共識及教師兩份榜單。改用共享 RankingResultLists，教師與學生顯示一致的兩份獨立列表。
- 全班共識以平均得分遞減、既有 finalRank 作為同分順序，顯示分數、名次及回答；教師列表嚴格使用已保存的 orderedGroupIds 次序。未改分數計算、排除本組或並列規則，也未替教師另創一套分數。
- 桌面並列、窄螢幕上下排列。學生個人排序及名次分布保留在收合內容；教師原始排序表保留。移除每份回答內混排老師與全班名次的呈現。
- 新增實際元件靜態渲染檢查：全班排序乙→甲、教師甲→乙時，各列表保持不同且正確的順序，輸入結果不被修改。最終單元測試 84 項通過。
- 重用元件後 CourseWorkspace 檔案由 1587 降至 1543 行，品質基線同步下修。
