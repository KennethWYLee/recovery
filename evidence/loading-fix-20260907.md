# 課程重新整理後的載入處理

2026-09-07：使用者回報重新整理後停在載入畫面，已授權修正與部署。

## 觀察與限制

- 讀取課程頁面、身分取得、CourseWorkspace、useWorkspaceLoader、API 回應解析、既有測試、PROJECT.md 與正式 Worker 記錄。
- 正式紀錄在 04:30 UTC 前後出現課程 session GET 被取消，後續同一路徑持續回傳 200；未找到相應的伺服器 500。
- 目前瀏覽器的 test 課堂已能顯示，部署前重新整理也成功。因此尚不能認定使用者當次卡住的唯一原因。
- 程式確認有兩個缺口：第一次 fetch 沒有逾時限制；背景更新會取消任何未完成的載入。前者可使畫面一直等待，後者會讓慢速背景請求反覆被中斷。

## 修改

- useWorkspaceLoader：15 秒中止未完成的讀取，顯示連線提示並提供既有「重新載入」按鈕；明確區別逾時與被新請求取代。
- 背景更新遇到尚未完成的請求即略過；手動重新載入仍可取代舊請求。
- tests/classroom-workspace-loader.test.mjs：以實際載入函式、受控 fetch 與計時器驗證逾時後重試、背景更新不打斷請求、舊請求不清除新載入狀態。React hooks 僅以最小測試替身執行，並非完整瀏覽器渲染測試。
- package.json：將三項測試加入單元測試指令。

## 驗證

- npm run test:unit：80 項通過。
- 最後調整 URL 參數組裝後重跑新測試：3 項通過。
- npm run gate:static：型別、lint 與程式品質檢查通過。
- npm run gate:build：正式建置與 4 項產物檢查通過。
- 正式課堂僅讀取與重新整理；未改動學生回答、排序或公布狀態。
- 此次未重跑與載入函式無關的資料庫、API 全流程與 mutation 測試。
- 以此次提交保存通過驗證的來源，再推送 Sites 來源庫及部署；部署結果由 Sites 回傳狀態確認。
