# 課堂代碼常駐

2026-09-07，依使用者要求，教師進入課程後須持續看得到本次課堂代碼。

- 已讀取 CourseWorkspace、既有頂端列與手機樣式、PROJECT.md、Sites 建置與部署規範。
- 原先代碼僅在報到階段的 QR 區域呈現；改在 CourseWorkspaceHeader 從目前 session.joinCode 顯示，與問題階段無關。
- 沿用 position: sticky 的頂端列，捲動後仍可看到；窄畫面代碼另排一列。
- 提供複製按鈕，僅在剪貼簿寫入成功後提示已複製，失敗則提示直接選取代碼。
- 課堂尚未建立時不顯示代碼；不更動報到、回答、排序、公布或資料庫。
- 將教師頂端列抽出，依品質檢查要求降低 CourseWorkspace 的行數基線。
- 驗證：TypeScript、ESLint 通過；下修基線後 quality:code 通過；正式建置與 4 項既有產物檢查通過。
- 本次為介面調整，未新增單元測試，也未重跑資料庫／API 全流程測試。手機配置依 CSS 實作，未進行實機測試。
- 以此次提交保存已驗證來源並部署到原 Sites 正式站，完成狀態由部署服務確認。
