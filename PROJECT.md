# Project Contract

## 產品範圍

本系統用於大學課堂。教師建立課程與課堂，學生輸入本次課堂代碼後加入。教師完成分組後，可在同一堂課內連續進行多個「小組作答、教師展示、個人排序、公布結果」活動。

不加入自動評分、社群按讚、正式成績換算或複雜遊戲化功能。

## 權限

- 系統管理員：管理課程、課堂、分組、問題、流程、結果與帳號審核。
- 學生：加入已開放的課堂、查看自己的組別、小組作答、提交個人排序及查看已公布結果。
- 只有小組指定代表可以編輯共同回答。
- 依 2026-09-07 使用者要求，小組回答至少一個非空白字元即可送出，單一數字（例如 0）也有效。
- 建立隨機平均分組時，每組同時隨機指定一位代表；教師可以在開始作答前調整代表。
- 學生不能查看其他學生的個人排序。
- 投影或學生畫面不顯示學生姓名與學號形式的個人排序資料。

## 核准用語

- 發言人：依 2026-09-07 使用者要求，在教師下方學生表格中標示及調整各組目前負責送出小組回答的指定代表；沿用既有作答代表權限，每組一人。不表示其他組員未曾發言。

- `course`／課程：一學期的教學單位。
- `class session`／本次課堂：某次上課的即時活動及分組範圍。
- `class code`／課堂代碼：學生加入本次課堂使用的六位代碼。
- `question`／問題：本次課堂內可重複新增的作答與排序單位。
- `question bank item`／問題庫項目：隸屬單一課程、具有單一主題分類與草稿／可使用狀態、可在不同課堂重複選用的問題與排序判準；選用後複製為該次課堂問題。
- `group response`／小組回答：每組由指定代表提交的一份共同回答。
- `complete ranking`／完整排序：每位學生將包含自己組在內的所有回答排列成不重複且不遺漏的名次；計算共識時移除自己組，再依剩餘順序重新編排名次。
- `published result`／已公布結果：排序已鎖定並由教師正式公開的結果。
- `group snapshot`／分組快照：問題開放作答時保存的組別歸屬，用於判斷自己組及保留歷史一致性。
- `synthetic demo data`／虛擬示範資料：明確標示、非真實學生或課堂活動的參考資料。

## 資料原則

- D1為課程、課堂、問題、分組、回答、排序與稽核紀錄的唯一正式資料來源。
- 課程名單是課程範圍的自動核准依據。符合有效名單的 `@ntub.edu.tw` 帳號自動取得系統與該課程存取權；名單外帳號仍須由系統管理員審核。
- Excel 或 TXT 檔案只在教師瀏覽器轉換為結構化名單；系統保存驗證後的學號、NTUB 信箱、選填姓名及匯入來源，不保存原始檔案。
- 問題公布後不覆寫原始排序；需要更正時應保留舊版本與原因。
- 問題鎖定但尚未公布時，管理員可重新開放個人排序，保留既有提交與分組快照，並記錄操作；已公布或已封存的問題不得重新開放。
- 修改或移除問題庫項目不得改寫先前課堂已建立的問題內容。
- 修改組別只影響作答中的問題或後續問題，不回寫已完成問題的分組快照。
- 依 2026-09-07 使用者要求，教師可在學生表格選擇「新增一組」，按既有最大組別編號加一建立組別並移入該生；新組只有該生時自動指定為發言人。沿用最多 20 組限制，新增及移動同一次完成。
- 問題開始作答後才加入的學生是該題觀察者，不取得該題分組快照或草稿；其組別從下一題開始納入。
- 排序進行中只顯示完成進度，不向學生顯示暫時名次。
- 依 2026-09-07 使用者修正，公布結果以本題實際收到的有效學生排序計算，不設固定三人門檻；至少須有一份有效學生排序，並沿用教師先完成排序的要求。未提交者不計入評選，教師畫面列出實際提交份數。
- 每份完整排序先移除排序者自己的組別並重新編排名次，再依回答數換算分數：第一名為最高分，最後一名為 1 分；各組以收到的平均分數由高到低排名。
- 平均分數相同時，依第一名票數、第二名票數及後續名次票數依序比較；所有名次分布完全相同才並列。
- 學生在答案展示與個人排序階段只看到匿名回答；伺服器不得傳回組名、組員或代表身分。結果公布後仍以回答內容為主，教師才可決定是否顯示組名。
- 學生排序與全班結果都由上而下呈現，越上方代表個人判斷或全班共識越高；名次分布預設收合，避免統計資訊壓過回答內容。
- 依 2026-09-07 使用者修正，公布結果分成全班共識與教師排序兩份獨立列表：前者依平均得分由高到低，後者依教師保存的名次順序；桌面並列、窄螢幕上下排列，不在每份回答內混列兩者名次。
- 學生可在目前問題與已公布的歷史問題間單選切換；已封存但曾公布的結果仍可查看，未公布即封存的內容不可見。
- 學生比較自己的排序與全班共識時，先移除自己的組別並重新編排名次；本組回答明確標示為不列入共識計算。
- 學生測試模式只允許管理員在內建示範課程選擇固定的虛擬學生；身分替換及操作權限均由伺服器驗證，正式課程不得使用。
