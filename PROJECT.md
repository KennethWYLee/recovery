# Project Contract

## 產品範圍

本專案是大學課堂使用的「小組回答與全班完整排序系統」。目前產品入口為 `/courses`；系統管理員的帳號審核入口為 `/access-review`。

目前已完成：

1. 課程與登入審核。
2. 今日課堂、QR Code 報到、平均隨機分組及教師拖曳調整。
3. 固定組數下的遲到補入、小組代表作答與階段鎖定。
4. 排除自己組的個人完整排序、結果圖表、原始排序與 CSV 匯出。

不得自行加入 AI 評分、AI 偵測、社群、按讚或複雜遊戲化功能。

## 身分與存取

- 固定系統管理員為 `wy.lee@ntub.edu.tw` 與 `kenneth.wy.lee21@gmail.com`。
- 其他帳號必須以 `@ntub.edu.tw` 為完整網域，並經系統管理員核准後才可進入。
- 待審核或遭拒絕的帳號不能讀取課程資料。
- 正式環境只接受託管平台提供的已驗證身分；本機開發身分僅能在 localhost 或 development 環境使用。
- 存取狀態及管理員操作保存在 D1，不以瀏覽器狀態作為授權依據。

## 課堂排序規則

- 每組只提交一份共同回答，只有一位指定代表可編輯。
- 個人排序必須包含全部可評回答，不得漏排、重複或並列。
- 每位學生首次看到的回答順序應隨機排列。
- 預設排除學生對自己組的排序，再重新編排名次並計算平均名次。
- 平均名次越小，整體排名越高；同分時依第 1 名、第 2 名至第 N 名的獲得次數依序判定，分布完全相同才並列。
- 同儕排序不自動換算成正式成績。
- 必須保存個人原始排序與修改版本。教師作廢紀錄屬下一階段管理功能。

## 資料與用語

- `course`：課程。
- `class session`：一次課堂活動。
- `question`：教師在活動中建立的題目。
- `group response`：一組共同提交的一份回答。
- `complete ranking`：學生對全部可評回答形成的不重複完整名次。
- `access request`：NTUB 帳號首次登入時建立、等待管理員處理的申請。
- `allowlist`：已核准登入的帳號集合。

課程與存取審核資料的正式來源是 D1；瀏覽器資料只能用於介面狀態，不能取代伺服器端權限判定。

## 驗證基準

- 每次修改至少執行受影響的單元測試、型別檢查與 ESLint。
- 交付前執行 `npm run gate:ci`，並檢查正式建置產物不含內部開發文字或已移除產品的介面與 API。
- 不得把成功建置等同於功能已完成；README 必須如實區分已實作與待開發範圍。
