# Continuity Ops 專案契約

## 產品定位

Continuity Ops 是單一組織部署的服務事件指揮與復原平台，提供 IT 維運、SRE、資安應變與服務團隊共同處理真實營運事件。正式產品介面與營運 API 只呈現完成營運任務所需的資料；開發對話、prompt、評分項目、配分、課程脈絡、學生文件、內部品質對照與交付管理資訊不得進入產品介面或公開部署產物。

產品資訊架構、互動原則與設計限制見 [產品設計基準](docs/product-design.md)。該文件是設計依據，不是部署或驗證證據。

## 核心使用者與任務

- 系統管理員：管理組織成員與角色；系統禁止停用或降級最後一位啟用中的管理員。
- 事件指揮官：組織角色為 `commander` 且具有本事件啟用中的 `incident_commander` 指派時，協調事件角色、控制高風險狀態轉換及確認復原；`admin` 是唯一不需要事件指派的全域寫入例外。
- 應變人員：組織角色為 `responder` 且具有本事件啟用中的 `responder` 或 `service_owner` 指派時，調查、提出假設、執行處置並留下驗證證據。
- 溝通負責人：`commander` 必須具有本事件啟用中的 `incident_commander` 或 `communications_lead` 指派，才可建立、編輯、核准及標記發布通訊；`responder` 可用 `responder`、`service_owner` 或 `communications_lead` 指派建立及編輯草稿，但只有 `communications_lead` 可核准及標記發布。`observer` 與 `auditor` 不會因事件指派取得寫入權限。
- 服務目錄維護者：由具 `service:write` 的系統管理員或事件指揮者建立及維護服務、SLO、Runbook 連結與生命週期。`service_owner` 是事件指派角色，不等於服務目錄寫入權限。
- 觀察者與稽核人員：以唯讀方式查閱營運總覽、全部事件、服務、稽核紀錄及自己的存取政策；不能取得成員目錄，也不能執行任何寫入。本版沒有資料匯出端點。

核心流程為：服務與責任建檔 → 宣告共享事件 → 指派事件角色 → 調查、處置及追蹤工作 → 建立、核准與標記事件通訊 → 進入監控並留下本次監控週期的驗證證據 → 通過復原門檻 → 解決或重新開啟 → 完成事後檢討與改善追蹤。

## 本版邊界

- 每次部署服務一個組織；不在本版宣稱完成多租戶隔離。
- 正式環境使用平台轉送的已驗證使用者身分；本機開發身分只允許 localhost 且必須由環境設定明確提供。
- 正式環境先採用既有會員資格；既有使用者或會員若為 `suspended`，不會因再次登入而恢復。正規化後 Email 網域精確為 `ntub.edu.tw` 的已驗證帳號，首次登入先建立安全的唯讀會員；每次從登入入口進入時，可選擇 `commander`、`responder`、`observer` 或 `auditor`。`admin` 不列入選項，只能由既有授權或部署設定指派。若使用者仍有進行中的事件責任，系統會拒絕不相容的角色切換。其他網域未受邀帳號回傳 403。桌面登入入口另提供指向同站 `/role-selection` 的手機 QR Code；QR 不含身分資料或可重用憑證，窄螢幕不顯示。
- `observer` 與 `auditor` 只能以 `GET` 讀取營運總覽、全部事件、服務、稽核及自己的存取政策。伺服器拒絕其所有 `POST`、`PUT`、`PATCH` 與 `DELETE`；存取政策回應不含成員目錄，稽核頁不顯示 actor email。`admin` 仍可查看 actor email。
- 系統記錄與協調變更，但不直接執行生產環境指令或保存生產憑證。
- Cloudflare D1 保存結構化營運資料；附件、SIEM、外部狀態頁、Email 與訊息平台整合尚未實作，不得宣稱已連線或已送達。
- 通訊的 `published` 是系統內核准與標記狀態，不是外部傳送或送達回執。
- 本版沒有原始 Log 上傳、附件、資料匯出或外部遙測匯入端點。系統另保存 API 自己產生的結構化請求紀錄，只含 route template、方法、結果、問題代碼、延遲、request ID 與版本，不保存 request body、權杖、Cookie、Email 或原始資源 ID。驗證事件仍只保存 HTTPS 參考連結、來源標籤、觀測起訖時間、選填 SHA-256 digest 與說明文字。
- 服務健康遙測尚未接入時，API 明確回傳 `operationalStatus: unknown`、`telemetryStatus: unavailable`、`sampleSize: 0` 與 `sloAttainment: null`，不得以「沒有事件」推論服務正常。
- 舊版演練資料結構視為 v1 歷史資料，不得成為 v2 專業事件資料模型。

## 技術與品質基準

- 共享事件必須有版本控制、冪等請求、可歸責身分、角色授權、時間軸及追加式稽核紀錄。相同 actor、操作範圍、Idempotency-Key 與 request payload 可重播已保存回應；相同 key 配不同 payload 必須拒絕。
- 事件時間軸、資安稽核與平台遙測分開保存；稽核紀錄不依賴選擇性分析同意。
- 高風險操作在伺服器端重新驗證權限；所有寫入驗證來源、內容長度、狀態轉換與資源關係。
- 組織角色與事件角色使用明確相容矩陣。API 與 D1 都會拒絕不相容的新指派；成員若仍有不相容的啟用中指派，也不得先變更角色或停用以繞過授權。
- 通訊依 `draft → reviewed → published` 的順序轉換。只有草稿可以編輯；已核准內容不得改寫，已發布紀錄不可更新或刪除。利害關係人與公開通訊在核准前，必須具有晚於核准時間的 `nextUpdateAt`，或從第一個字元使用大小寫不拘的獨立 `[FINAL]` 標記；標記後必須是空白或訊息結束。發布時再次確認排程仍在未來；事件進入 `resolved`、`closed` 或 `cancelled` 後不得再標記發布。
- 事件狀態變更、服務淘汰或重新啟用、角色撤銷與交接、工作完成或取消、成員降權或停用，以及通訊核准與標記發布已有相稱的確認、風險提示或理由欄位；任何前端確認都不能取代伺服器與資料庫控制。
- 系統觀測以流量、HTTP 結果與延遲回答營運問題，並提供路徑、問題代碼、request ID、部署版本及資料來源。正式執行資料與模擬資料必須分開計數；模擬資料只供本機演練與圖表查核，不得稱為正式監控結果、外部使用者意見或生產證據。

### 核准用語

- `系統觀測`：根據系統自行產生的結構化請求紀錄，檢視流量、錯誤、延遲與問題來源。
- `結構化請求紀錄`：欄位固定且不含請求內容或身分資料的 API 執行摘要；不是原始 Log、稽核紀錄或使用者回饋。
- `模擬資料`：由可重現的本機程序產生、欄位明確標示 `simulated` 的操作演練資料；不得改稱真實資料或外部證據。
- 指派撤銷採軟撤銷並保存結束時間與操作者。撤銷最後一位事件指揮官時，合格接任者的指派、原指派撤銷、時間軸與稽核必須在同一批次完成。
- 事件由 `monitoring` 轉為 `resolved` 前，資料庫必須同時確認：已定義復原判定條件、存在進入本次 `monitoring` 後建立的 `verification` 紀錄，以及所有 `critical` 工作皆已完成或取消。重新進入 `investigating` 會重設監控週期時間，舊週期證據不能用於下一次復原。
- 工作建立及未完成狀態可不填證據；改為 `completed` 前，必須提供具有主機名稱的 HTTPS 證據網址。`critical` 工作改為 `cancelled` 時必須提供 8–1000 字理由；原工作為 `critical` 時，同一更新內先降級也不能略過理由，理由一旦記錄不得移除或改寫。API 與 D1 都會維持這些條件。系統只保存網址與取消理由，不擷取或驗證外部內容，且完成或取消狀態不能單獨支持成果正確的聲明。
- 事後檢討只能在事件為 `resolved` 或 `closed` 時儲存，支援 `draft` 與 `completed`。完成版必須具備六個必要段落；事件重新開啟時，完成版會自動回到草稿並留下時間軸與稽核紀錄。
- 服務與成員更新皆使用 optimistic version；服務支援 `active` 與 `deprecated` 生命週期及不可變 slug，仍有未結案事件時不得淘汰服務。每次服務生命週期變更必須同時保存 8–1000 字原因、具 `service:write` 的啟用中操作者與時間；既有歷史若未曾保存理由，migration 必須保留缺漏狀態，不得推測或補寫。成員更新的 `expectedVersion` 過期時必須拒絕，不得以最後寫入覆蓋並行管理決定。
- 冪等回執有效 24 小時；建立新回執時會在同一組織內有界清除最多 100 筆過期回執，並清除與本次 key 相符的過期回執。這不是一般資料保存排程。
- API 以不含 request body 與原始資源 ID 的 JSON 結構化遙測記錄 route template、request ID、方法、狀態、問題代碼、延遲、API／schema／部署版號。未設定 `CONTINUITY_OPS_DEPLOYMENT_VERSION` 時記為 `unversioned`；staging 與 production 遇到此值必須停止發布。
- 畫面可見且沒有寫入進行時，選取事件的明細、時間軸與通訊每 8 秒輪詢；總覽每 30 秒輪詢。頁面重新可見時會立即讀取事件明細。這些機制不是即時推播或外部送達證據。
- D1 保存組織的 IANA 時區；時間顯示與 `datetime-local` 輸入皆依該時區轉換。設定缺漏或無效時採 UTC，日光節約時間中不存在或重複的地方時間會被拒絕。
- `view`、`incident` 與 `tab` 查詢參數可建立深層連結、還原畫面位置及支援瀏覽器前進、後退。URL 是不可信輸入，不得包含秘密，且不能繞過伺服器授權。
- 介面以 WCAG 2.2 AA 為目標，使用原生語意元素、鍵盤可操作控制、文字與圖示共同表達狀態。
- 事件流程參考 NIST SP 800-61 Rev. 3 與 Google SRE Incident Management Guide；應用程式安全驗證參考 OWASP ASVS 5.0。

## 情境導覽

狀態：已實作第一條五步導覽「系統更新後，部分功能變慢或出錯」。使用者從營運頁面右上角的「導覽」選擇情境，再依序查看異常、更新時間、趨勢、API／Log 與恢復條件。

- 情境資料固定且明確標示為受控模擬資料，只在導覽期間取代系統觀測畫面的讀取結果；不寫入 D1，也不修改正式營運紀錄。
- 更新時間只是線索。導覽要求同時比對錯誤率、回應時間、API 路徑、問題代碼、部署版本、Request ID 與處理後結果，不以時間接近直接宣稱因果關係。
- 每條情境維持單一判斷目標與短步驟。新增情境時必須使用獨立、可重現且內部數字一致的資料，並為每個畫面目標建立穩定選擇器。
- 只有具有系統觀測讀取權限且營運資料已載入的使用者可以啟動。切換畫面時若目標未出現，導覽會停止並返回原本畫面。
- 導覽由使用者主動啟動，可用關閉按鈕或鍵盤離開；系統不保存完成狀態，也不把瀏覽器狀態當成帳號層級證據。

## 完成定義

本版至少要能由多個具名角色在同一事件上完成宣告、調查、處置、工作追蹤、結構化通訊、驗證、解決、重新開啟及事後改善；每項操作可追溯至人、時間、資源、版本與結果。型別檢查、Lint、單元測試、Gherkin 驗收與風險導向辨錯、授權負例、遷移測試、受控故障注入、品質指標、建置與整合檢查全部通過，才可列為本機發布候選版。API、黑箱、瀏覽器、有界負載、故障後復原與隔離重建的詳細結果，由 assurance evidence 另行紀錄。本機發布候選版不等於已部署、已完成外部通訊整合、外部使用驗證、獨立資安查核、完整 WCAG 符合性檢查或遠端備份還原演練。
